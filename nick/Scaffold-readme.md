On the scaffold folder, run:

yarn install


Then on one console to run the chain run:

yarn chain

(it will create a local chain)


To deploy contracts run:

yarn deploy


To start website runs:

yarn start


Then go to:

http://localhost:3000